# requirements.txt — Зависимости бота
# =====================================
#
# Установка: pip install -r requirements.txt
#
# ccxt           — библиотека для работы с биржами
# ccxt[pro]      — расширение с WebSocket поддержкой
# numpy          — математика (массивы, вычисления)
# aiohttp        — асинхронные HTTP запросы (нужен для ccxt.pro)

ccxt>=4.0.0
numpy>=1.26.0
aiohttp>=3.9.0
